--- hb-install-hppa-kernel.xml	2006-01-09 14:01:12.000000000 +0100
+++ hb-install-hppa-kernel-1.xml	2006-01-09 14:01:12.000000000 +0100
@@ -8,8 +8,8 @@
 
 <sections>
 
-<version>1.22</version>
-<date>2005-11-21</date>
+<version>1.23</version>
+<date>2006-01-06</date>
 
 <section>
 <title>Strefa czasowa</title>
@@ -46,20 +46,26 @@
 </p>
 
 <p>
-Dla architektury HPPA nie są już dostępne źródła serii 2.4. Trzeba korzystać z
-<c>hppa-sources</c>. Aby skorzystać z jądra 2.4, należy instalować Gentoo z
-użyciem Internetu, gdyż jądro to nie znajduje się już na płytach
-instalacyjnych. Flaga USE="-doc" zapobiegnie instalacji niepotrzebnych na tym
-etapie zależności, takich jak np. xorg-x11.
+Dla architektury HPPA w portage znajduje się <c>hppa-sources</c>. Domyślnie
+bazują one na kernelu serii 2.6. Jeżeli będziemy chcieli zainstalować kernel
+serii 2.4, będziemy musieli zainstalować Gentoo z działającym połączeniem
+internetowym, gdyż wyżej wymienionych źródeł nie znajdziemy na płycie
+instalacyjnej. Flaga <c>USE="-doc"</c> uniemożliwi zainstalowanie xorg-x11 i 
+innych zbędnych na tym etapie zależności. Flaga <c>USE="symlink"</c> nie jest
+konieczna w przypadku nowej instalacji, jednak zapewnia w późniejszym okresie
+tworzenie prawidłowych dowiązań symbolicznych <path>/usr/src/linux</path>.
 </p>
 
 <pre caption="Instalowanie źródeł jądra">
-# USE="-doc" <i>emerge hppa-sources</i>
+# USE="-doc symlink" <i>emerge hppa-sources</i>
 </pre>
 
 <p>
 W katalogu <path>/usr/src</path> powinieneś mieć mniej więcej taki symlink, o
-nazwie <path>linux</path>, wskazujący na źródła Twojego aktualnego kernela:
+nazwie <path>linux</path>, wskazujący na źródła Twojego aktualnego kernela. W
+tym wypadku wskazuje na źródła <c>hppa-sources-2.6.12.2-pa2</c>. W komputerze
+użytkownika może być to inna wersja, dlatego należy mieć to na uwadze.
+
 </p>
 
 <pre caption="Podgląd symlinka do źródeł kernela">
@@ -68,18 +74,6 @@
 </pre>
 
 <p>
-Jeśli informacje z symlinka nie pokrywają się z rzeczywistością (np. wskazuje na
-inne niż Twoje źródła kernela) to będziesz musiał go poprawić zanim przejdziesz
-do dalszych części tego dokumentu.
-</p>
-
-<pre caption="Zmiana dowiązania symbolicznego do źródeł jądra">
-# <i>rm /usr/src/linux</i>
-# <i>cd /usr/src</i>
-# <i>ln -s linux-2.6.12.2-pa2 linux</i>
-</pre>
-
-<p>
 Pora na skonfigurowanie i skompilowanie źródeł jądra. Możesz użyć do tego
 programu <c>genkernel</c>, który zbuduje uniwersalne jądro, takie jak np.  to
 używane przez płyty instalacyjne. Możesz też przeprowadzić cały proces ręcznie
@@ -166,16 +160,14 @@
 których zamierzasz używać. Jeśli tego zaniedbasz Gentoo nie będzie w stanie
 zamontować partycji, a czasem nawet się nie uruchomi. Pamiętaj również, aby
 włączać te sterowniki na stałe do jądra, broń Boże nie kompiluj ich jako
-modułów. Przy okazji zaznacz też <c>Virtual memory</c>, <c>/proc file system</c>
-i <c>/dev file system</c> + <c>Automatically mount at boot</c>:
+modułów. Przy okazji należy zaznaczyć również <c>Virtual memory</c>, oraz 
+<c>/proc file system</c>:
 </p>
 
 <pre caption="Wybór potrzebnych systemów plików">
 File systems ---&gt;
   Pseudo Filesystems ---&gt;
     [*] /proc file system support
-    [*] /dev file system support (OBSOLETE)
-    [*]   Automatically mount at boot
     [*] Virtual memory file system support (former shm fs)
 
 <comment>(Wybierz jedną lub kilka z tych opcji zależnie od używanych systemów
@@ -277,22 +269,14 @@
 </pre>
 
 <p>
-Kiedy jądro skończy się kompilować przekopiuj jego obraz do katalogu
-<path>/boot</path>. W przykładzie zakładamy, że właśnie zostały skompilowane
-źródła <c>hppa-sources-2.6.12.2-pa2</c>.
+Kiedy jądro skończy się kompilować kopiujemy jego obraz do katalogu
+<path>/boot</path>. W nowszych kernelach obraz nazywa się
+<path>vmlinux</path> zamiast <path>vmlinux.gz</path>. Należy o tym pamiętać w
+czasie gdy będziemy kopiować kernel.
 </p>
 
 <pre caption="Instalowanie kernela">
-# <i>cp vmlinux /boot/kernel-2.6.12.2-pa2</i>
-</pre>
-
-<p>
-Zwykle mądrą decyzją jest skopiowanie tam również pliku konfiguracyjnego, który
-posłużył do zbudowania jądra.
-</p>
-
-<pre caption="Tworzenie kopii zapasowej konfiguracji jądra">
-t# <i>cp .config /boot/config-2.6.12.2-pa2</i>
+# <i>cp vmlinux /boot/&lt;kernel-version&gt;</i>
 </pre>
 
 <p>
